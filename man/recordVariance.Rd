% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varianceLedger.R
\name{recordVariance}
\alias{recordVariance}
\title{Record variance decomposition results}
\usage{
recordVariance(
  object,
  assayName,
  formula,
  result,
  method = "variancePartition"
)
}
\arguments{
\item{object}{A \code{BatchVariaData} object}

\item{assayName}{Character. Name of the assay associated with the result}

\item{formula}{Model formula used for variance decomposition}

\item{result}{Data frame containing variance decomposition results.
Must include at least \code{source}, \code{term}, and
\code{variance_fraction} columns}

\item{method}{Character. Name of the variance decomposition method}
}
\value{
Updated \code{BatchVariaData} object with appended variance record
}
\description{
Stores the output of a variance decomposition method in the internal
variance ledger of a \code{BatchVariaData} object. Each entry captures
the assay, model formula, method used, and resulting variance summary.
}
\details{
Results are appended to \code{metadata(object)$variance_history} as a list of
entries. Each entry contains:
\itemize{
\item \code{assay} – assay name
\item \code{formula} – model formula
\item \code{method} – variance decomposition method
\item \code{result} – variance summary data frame
}
}
\examples{
set.seed(1)
bv <- exampleBatchVaria(nGenes = 100)
res <- data.frame(
    source = "anova",
    term = c("model", "residual"),
    variance_fraction = c(0.3, 0.7)
)
bv <- recordVariance(
    bv,
    assayName = "raw",
    formula = ~batch,
    result = res,
    method = "anova"
)
length(varianceHistory(bv))

}
