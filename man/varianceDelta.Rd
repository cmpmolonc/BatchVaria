% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation-modules.R
\name{varianceDelta}
\alias{varianceDelta}
\title{Compute variance redistribution relative to baseline}
\usage{
varianceDelta(
  object,
  assays = NULL,
  method = "anova",
  formula = NULL,
  baseline = NULL
)
}
\arguments{
\item{object}{A \code{SummarizedExperiment} meeting BatchVaria's
requirements. See \link{BatchVaria-requirements}.}

\item{assays}{Character vector of assay names (default: all assays)}

\item{method}{Variance estimation method}

\item{formula}{Model formula to compare under. Required when the chosen
method has results for more than one formula, since a delta between
decompositions of different models is not interpretable.}

\item{baseline}{Baseline assay name. When \code{NULL} (the default) it
is inferred from the correction ledger, falling back to \code{"raw"}.}
}
\value{
data.frame with columns: assay, method, term, delta
}
\description{
Calculates the change in variance explained by each model term
for one or more assays relative to a baseline assay.
}
\examples{
set.seed(1)
bv <- exampleBatchVaria(nGenes = 100)
bv <- profileVariance(bv, ~batch, methods = "anova")
bv <- runCorrection(bv, method = "combat", batch = "batch")
bv <- profileVariance(bv, ~batch, assayName = "raw_combat", methods = "anova")
varianceDelta(bv)

}
